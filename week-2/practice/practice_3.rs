fn main() {
	let fees = 25_000; // 25_000 is a value stored in the variable 'fees'

	let salary: f64 = 35_000.00; // likewise here however, the 'f64' shows that it is a decimal value with a memory of 64bits which should be equal to 2.1 billion and above.

	println!("fees in {} and salary is {}", fees, salary);
}